export class Memory {
    description: string;
    strength: number;
  
    constructor(description: string, strength: number) {
      this.description = description;
      this.strength = strength;
    }
}